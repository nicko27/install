name: Copie de comptes utilisateurs
description: "Copie de comptes utilisateurs vers un support externe"
version: 1.0
category: Système
multiple: true
icon: 🏠
config_fields:
  home_src_dir:
    type: directory
    label: Dossier home source
    default: /home
    required: true
    not_empty: true

  user_select:
    type: checkbox_group
    label: Sélectionner les utilisateurs à copier
    depends_on: home_src_dir
    dynamic_options:
      script: get_users.py
      global: true
      function: get_users
      args:
        - field: home_src_dir
          param_name: home_dir
      value_key: username
      label_key: description

  user_all:
    type: checkbox
    label: Copier tous les comptes utilisateurs
    # Désactivé si aucun utilisateur n'est disponible
    enabled_if:
      field: user_select
      value: has_options

  home_dst_dir:
    type: directory
    label: Dossier home destination
    required: true
    not_empty: true

  machine_dir:
    type: checkbox
    label: Créer un dossier avec le nom de machine dans la destination
    default: true