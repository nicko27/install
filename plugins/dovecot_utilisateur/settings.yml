name: Dovecot - Configuration utilisateurs
description: Configure tous les prefs.js d'un ordinateur pour les dossiers locaux unités
version: 1.0
category: Système
multiple: true
ssh_debug: true
icon: 🛠️
# Ajout du support pour l'exécution distante
remote_execution: false
needs_sudo: false
config_fields:
  sms:
    type: text
    label: Nom de machine de la sms
    required: true
    validate: no_spaces
    default: "ggd027sf"
