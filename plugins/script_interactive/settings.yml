name: Script Interactif
description: Exécute un script shell interactif avec des réponses automatiques
version: 1.0
category: Installation
multiple: false
icon: 📄

config_fields:
  install_web_server:
    type: checkbox
    label: Installer un serveur web
    description: Installer un serveur web avec support PHP optionnel
    default: true
    required: true
    group: Type d'installation

  web_server_port:
    type: text
    label: Port du serveur web
    description: Port d'écoute du serveur web (1-65535)
    default: "80"
    placeholder: "Ex: 80, 8080"
    validate: number
    min: 1
    max: 65535
    condition: install_web_server
    required: true
    not_empty: true

  enable_php:
    type: checkbox
    label: Activer PHP
    description: Installer et configurer le support PHP
    default: true
    condition: install_web_server

  php_version:
    type: select
    label: Version de PHP
    description: Version de PHP à installer
    options:
      - "7.4"
      - "8.0"
      - "8.1"
    default: "8.1"
    condition: enable_php
    required: true

  db_type:
    type: select
    label: Type de base de données
    description: Type de serveur de base de données à installer
    options:
      - mysql
      - postgresql
    default: mysql
    condition: "!install_web_server"
    required: true

  db_port:
    type: text
    label: Port de la base de données
    description: Port d'écoute de la base de données (1-65535)
    placeholder: "Ex: 3306 (MySQL), 5432 (PostgreSQL)"
    validate: number
    min: 1
    max: 65535
    condition: "!install_web_server"
    required: true
    not_empty: true
    dynamic_default:
      mysql: "3306"
      postgresql: "5432"

  db_root_password:
    type: password
    label: Mot de passe root
    description: Mot de passe administrateur (minimum 6 caractères)
    default: "root123"
    min_length: 6
    condition: "!install_web_server"
    required: true
    not_empty: true
    validate: no_spaces

  confirm_config:
    type: checkbox
    label: Confirmer la configuration
    description: Confirmer et appliquer la configuration
    default: true
    required: true
